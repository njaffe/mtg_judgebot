# MTG JudgeBot — Python Dependencies
# Install: pip install -r requirements.txt

# === Core (required) ===
anthropic>=0.40.0          # Claude API client (chat/synthesis)
faiss-cpu>=1.7.4           # Vector similarity search
sentence-transformers>=3.0 # Local embeddings (all-MiniLM-L6-v2)
numpy>=1.24                # Array operations for embeddings
python-dotenv>=1.0         # .env file loading
requests>=2.31             # HTTP client (Scryfall API)

# === Web UI ===
streamlit>=1.30            # Streamlit web interface

# === External Sources (optional — only needed if ENABLE_EXTERNAL_SOURCES=true) ===
praw>=7.7                  # Reddit API client
google-api-python-client>=2.100  # Google Custom Search

# === Proxy / Docker mode (optional — only needed for Docker deployment) ===
# fastapi>=0.104
# uvicorn>=0.24
# httpx>=0.25
# tiktoken>=0.5
