# Proxy Service Requirements
# Dependencies for the FastAPI-based LLM proxy layer.
# This file is used independently of the root requirements.txt to keep the proxy container lightweight.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
redis==5.0.8
python-dotenv==1.0.1
tiktoken==0.7.0